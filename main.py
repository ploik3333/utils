# # from utils import *
#
# b = lambda b: bin(ord(b));
# c = lambda c, b=16: chr(int(c, b));
# d = {'0': c('200B'), '1': c('200C')};
# e = {d[a]: a for a in d}
#
# def whitespace_decode(text):
#     c('200D').join(["".join(map(d.get, b(a)[2:])) for a in text])
# def whitespace_encode(text):
#     return "".join([*map(lambda f: c("".join([*map(e.get, f)]), 2), text.split(c('200D')))])
# a = "this is some text"
# print(whitespace_decode(a))
# print(whitespace_decode(whitespace_encode(a)))








